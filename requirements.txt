# AdaptAble Backend — requirements.txt
# Tested on Python 3.11.x (Render's stable version)
# Every version is pinned to prevent "works locally, breaks on Render" surprises

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ── Supabase ──────────────────────────────────────────────────────────────────
supabase==2.10.0

# ── JWT validation ────────────────────────────────────────────────────────────
PyJWT==2.10.1
cryptography==43.0.3

# ── Email validation (REQUIRED for Pydantic EmailStr) ─────────────────────────
# Without this the app crashes at startup on Render with:
# "email-validator is not installed, run pip install pydantic[email]"
email-validator==2.2.0

# ── PDF extraction ────────────────────────────────────────────────────────────
pdfplumber==0.11.4
pdfminer.six==20231228

# ── HTTP client (async, for HuggingFace + Azure TTS) ─────────────────────────
httpx==0.27.2

# ── Configuration and validation ──────────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1